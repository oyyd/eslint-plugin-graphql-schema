{
  "name": "eslint-plugin-graphql-schema",
  "version": "0.1.0",
  "description": "GraphQL schema specific linting rules for ESLint",
  "main": "lib/index.js",
  "scripts": {
    "lint": "eslint src",
    "build": "npm run lint && babel src --out-dir lib",
    "dev": "mocha -w src/**/*.spec.js",
    "test": "npm run build && mocha lib/**/*.spec.js"
  },
  "keywords": [
    "eslint",
    "eslintplugin",
    "graphql",
    "graphql schema"
  ],
  "author": "oyyd <oyydoibh@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.23.0",
    "babel-eslint": "^7.1.1",
    "babel-preset-es2015": "^6.22.0",
    "eslint": "^3.15.0",
    "eslint-config-airbnb-base": "^11.1.0",
    "eslint-plugin-import": "^2.2.0",
    "expect": "^1.20.2",
    "mocha": "^3.2.0"
  },
  "peerDependencies": {
    "eslint": "^2.0.0 || ^3.0.0"
  }
}
